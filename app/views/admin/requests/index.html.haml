%h1 Solicitudes

#container-fluid
  #pad-wrapper  
    .row-fluid
      .span

.class.table-wrapper.user-table
  .row-fluid.head
    .span12
    .row-fluid.filter-block
      .pull-right
    .row-fluid
      %table.table.table-hover
        %thead
          %tr
            %th.span4
              Solicitante
            %th.span3
              %span.line>
              Fecha de solicitud
            %th.span2
              %span.line>
              Monto solicitado
            %th.span3.align-right
              %span.line>
              Correo Electrónico
        %tbody
          %tr
            %td
              /%img.img-circle.avatar.hidden-phone{:src => "img/contact-img.png"}/
              %span.subtext Manuel Corripio
            %td
              Jan 11, 2015
            %td
              10,000
            %td.align-right
              ejemplo@usuario.com

%table.table.table-bordered.table-striped
  %thead
    %tr
      %th ID
      %th Amount
      %th User
      %th Status
      %th Financing Time
      %th Financing Rate
      %th Created At
      %th       

  %tbody
    - @requests.each do |request|
      %tr
        %td= request.id
        %td= request.user.name
        %td= number_to_currency request.amount
        %td= request.status
        %td= pluralize request.financing_time, 'month'
        %td= number_to_percentage(request.financing_rate, precision: 0)   
        %td= request.created_at.strftime('%e %b %Y %H:%M:%S')
        %td
          = link_to 'Show', admin_request_path(request)
          = link_to 'Edit', edit_admin_request_path(request)
          = link_to 'Destroy', admin_request_path(request), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Request', new_admin_request_path